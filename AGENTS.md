# Agent Instructions

Use `AI_CONTEXT.md` as the canonical instruction and strategy document for this
repository.

## Required Workflow
- Read `AI_CONTEXT.md` before proposing or making changes.
- Preserve behavior contracts and guardrails defined there.
- If instructions appear duplicated across agent-specific files, treat
  `AI_CONTEXT.md` as the source of truth.

## Scope Note
This file is an adapter for tools that auto-load `AGENTS.md`.
