# GitHub Copilot Instructions

Use `AI_CONTEXT.md` in the repository root as the canonical project context.

## Expectations
- Respect the current daemon-centric architecture.
- Preserve runtime data contracts in `/data/` and generated scripts in
  `/modules/`.
- Keep root/deploy boot reliability and stock capability-gating behavior.
